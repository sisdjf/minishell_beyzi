


avant d'essayer d'execve une commande il faut verifier si ca n'est pas un builtin

        - int execve(char *path, char **argv, char **envp);


avant d'essayer de recuperer le path on verifie si la commande donne n'est pas deja le chemin absolu.
        si ca l'est on exec et on fait tte la demarche de chercher le path on return simplement apres

path le recuperer avec retrouver variable
    si le path n'est pas retrouve on return NULL + on print que command not found

argv // Il faut convertir le nom de la commade avec ses arguments de cette facon --> [ls] [-l] [NULL]
envp // pareil que argv en stockant toutes les lignes env_str dans un char **







